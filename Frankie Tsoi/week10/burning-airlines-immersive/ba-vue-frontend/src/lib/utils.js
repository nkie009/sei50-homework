//the below will also work
// const adder = (first, second) => {
//   return first + second;
// };

// export default adder; 

const adder = (first, second) => {
    return first + second;
  };
  
const subber = (first, second) => {
    return first - second;
  };

const bigNum = 123456789;

export {adder, subber};